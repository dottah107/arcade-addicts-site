<div class="topbar">
  <h2 id="aaHeroTitle">Member</h2>
  <div class="tabs">
    <button class="tabbtn" data-target="friends">Friends</button>
    <button class="tabbtn" data-target="gamertag">My Gamertag</button>
    <button class="tabbtn" data-target="upload">Upload</button>
  </div>
</div>
<!doctype html>
<html lang=""en"">
<head>
  <meta charset=""utf-8""/>
  <meta name=""viewport"" content=""width=device-width,initial-scale=1""/>
  <title>Member</title>
  <style>
    :root{
      --bg:#0b0e10; --panel:#111418; --line:#1f2730; --text:#e8f0f6;
      --muted:#99a7b3; --teal:#14b8a6; --teal-2:#0d9488; --ink:#64ffda;
      --shadow:0 6px 24px rgba(0,0,0,.35);
    }
    html,body{background:#000;color:var(--text);font:15.5px/1.5 system-ui,Segoe UI,Roboto,Arial;margin:0}
    .wrap{max-width:1200px;margin:24px auto;padding:0 16px}
    /* --- header panel --- */
    .header-grid{
      display:grid;gap:16px;
      grid-template-columns:repeat(12,1fr);
    }
    .card{
      background:var(--panel); border:1px solid var(--line); border-radius:14px;
      box-shadow:var(--shadow); padding:16px; position:relative; overflow:hidden;
    }
    .hero{
      background:linear-gradient(135deg,#0a0f12, #0b1f1e);
      border:1px solid #0b2a2a; border-radius:16px; padding:18px;
      display:flex; align-items:center; justify-content:space-between; gap:12px;
    }
    .hero h2{margin:0;font-size:18px;letter-spacing:.4px}
    .badge{display:inline-block;padding:3px 10px;border-radius:999px;background:#0b3d3d;color:var(--ink);font-weight:700;font-size:12px;border:1px solid #124a4a}

    /* layout placements */
    .col-4{grid-column:span 4}
    .col-8{grid-column:span 8}
    @media (max-width:900px){ .col-4,.col-8{grid-column:span 12} }

    /* controls */
    input,button{
      border-radius:12px; border:1px solid var(--line); background:#0d1116; color:var(--text);
      padding:10px 12px;
    }
    input:focus{outline:2px solid var(--teal)}
    .btn{cursor:pointer; font-weight:700}
    .btn.primary{background:var(--teal); color:#000; border-color:transparent}
    .btn.primary:hover{background:var(--teal-2)}
    .hint{font-size:12px;color:var(--muted)}
    ul{margin:8px 0 0 18px;padding:0}
    h3{margin:0 0 10px}

    /* avatar chips for “online friends” (optional later) */
    .chips{display:flex;gap:8px;flex-wrap:wrap}
    .chip{display:inline-flex;align-items:center;gap:8px;background:#0f1318;border:1px solid var(--line);border-radius:999px;padding:6px 10px}
    .avatar{display:grid;place-items:center;width:28px;height:28px;border-radius:999px;background:#0c1116;border:1px solid #1b2730;color:var(--ink);font-weight:800;font-size:12px}

    /* make inputs readable (dark theme) */
    #aaGamertag,#aaUploadTitle,#aaUploadFile{background:#0b1215;color:#fff;border:2px solid var(--teal)}
    #aaGamertag::placeholder,#aaUploadTitle::placeholder{color:#86fff0}

    /* lists */
    .cols{display:flex; gap:18px; flex-wrap:wrap}
    .col{min-width:260px; flex:1}
  </style>
<style id="aa-tabs-css">
  .topbar{display:flex;align-items:center;justify-content:space-between;gap:12px;background:linear-gradient(135deg,#0a0f12,#0b1f1e);border:1px solid #0b2a2a;border-radius:16px;padding:16px;margin:16px 0}
  #aaHeroTitle{margin:0;font-size:18px;letter-spacing:.3px}
  .tabs{display:flex;gap:8px;flex-wrap:wrap}
  .tabbtn{cursor:pointer;padding:10px 14px;border-radius:999px;border:1px solid #1e2a2f;background:#0f1318;color:#e8f0f6;font-weight:700}
  .tabbtn.active{background:#14b8a6;border-color:transparent;color:#000}
  .aa-panel{display:none;margin-top:14px}
  .aa-panel.active{display:block}
</style>
</head>
<body>
  <div class=""wrap"">

    <!-- HERO BAR -->
    <div class=""hero card"">
      <h2>Member Dashboard</h2>
      <span class=""badge"">arcade addicts</span>
    </div>

    <!-- TOP PANEL: three side-by-side cards -->
    <div class=""header-grid"">
      <!-- FRIENDS -->
      <section id=""aa-friends-section"" class=""card col-4"">
        <h3>Friends</h3>
        <div style=""display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-top:8px"">
          <input id=""aaFriendEmail"" placeholder=""Friend's email"" style=""flex:1;min-width:220px"">
          <button id=""aaSendReq"" class=""btn primary"">Send</button>
        </div>
        <div class=""cols"" style=""margin-top:12px"">
          <div class=""col"">
            <div class=""hint"">Incoming</div>
            <ul id=""aaIncoming""></ul>
          </div>
          <div class=""col"">
            <div class=""hint"">Outgoing</div>
            <ul id=""aaOutgoing""></ul>
          </div>
        </div>
      </section>

      <!-- MY GAMERTAG -->
      <section id=""aa-gamertag-section"" class=""card col-4"">
        <h3>My Gamertag</h3>
        <div style=""display:flex;gap:10px;align-items:center;flex-wrap:wrap;margin-top:8px"">
          <input id=""aaGamertag"" placeholder=""Enter gamertag"" style=""flex:1;min-width:220px"">
          <button id=""aaSaveGamertag"" class=""btn primary"">Save</button>
          <span id=""aaGTStatus"" class=""hint""></span>
        </div>
      </section>

      <!-- UPLOAD -->
      <section id=""aa-upload-section"" class=""card col-4"">
        <h3>Upload</h3>
        <div style=""display:grid;gap:8px;margin-top:8px"">
          <input id=""aaUploadTitle"" placeholder=""Title (optional)"">
          <input id=""aaUploadFile"" type=""file"">
          <button id=""aaUploadBtn"" class=""btn primary"" style=""width:140px"">Upload</button>
        </div>
      </section>
    </div>

    <!-- POSTS LIST -->
    <section class=""card"">
      <h3>Your recent posts</h3>
      <ul id=""aaUploadsList""></ul>
    </section>

    <!-- (optional later) Friends accepted / presence card can go here -->

  </div>

  <!-- scripts (SDK -> init -> app code) -->
  <script src=""/assets/vendor/supabase.min.js?v=ui1""></script>
  <script src=""/assets/supabase-init.js?v=ui1""></script>
  <script src=""/assets/social.js?v=ui1""></script>
  <script src=""/assets/member-wire.js?v=ui1""></script>
</body>
</html>



